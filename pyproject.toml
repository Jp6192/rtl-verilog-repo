[project]
name = "rtl-verilog-repo"
version = "0.1.0"
description = "Verilog RTL projects with cocotb tests"
requires-python = ">=3.12"
dependencies = [
    "cocotb>=1.9.0",
    "cocotb-test>=0.2.6",
    "pytest>=8.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
